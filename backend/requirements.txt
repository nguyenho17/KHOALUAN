# ==============================================================
# requirements.txt — ChatBot AI LexRAG++ (HuggingFace Production)
# ==============================================================

# ── Web Framework & Server ─────────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart
python-dotenv

# ── Database ──────────────────────────────────────────────────
# PostgreSQL driver (production — Neon.tech / Supabase)
psycopg2-binary

# SQL Server driver (chỉ cần khi dev local trên Windows)
# pyodbc

# SQLAlchemy ORM
sqlalchemy

# Tiện ích parse URL
urllib3

# ── Xử lý văn bản PDF ─────────────────────────────────────────
PyPDF2
pdfplumber
python-docx

# ── AI & Embedding ─────────────────────────────────────────────
# Bi-Encoder: intfloat/multilingual-e5-base
# Cross-Encoder: cross-encoder/ms-marco-MiniLM-L-6-v2
sentence-transformers

# Client gọi OpenRouter API (tương thích OpenAI SDK)
openai

# Google Gemini (dự phòng)
google-generativeai

# ── Vector Database ────────────────────────────────────────────
faiss-cpu

# ── Hybrid Search ──────────────────────────────────────────────
# BM25 sparse retrieval
rank_bm25

# Tách từ tiếng Việt cho BM25
pyvi

# ── Bảo mật ───────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
PyJWT==2.8.0
python-jose[cryptography]

# ── Tính toán & Công cụ ───────────────────────────────────────
numpy
pandas
openpyxl
tqdm

# ── Text Splitting ─────────────────────────────────────────────
langchain-text-splitters